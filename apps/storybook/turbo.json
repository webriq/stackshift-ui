{
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["storybook-static/**"]
    },
    "build-storybook": {
      "dependsOn": ["^build-storybook"],
      "outputs": ["storybook-static/**"]
    },
    "build-storybook-docs": {
      "dependsOn": ["^build-storybook-docs"]
    },
    "storybook": {
      "dependsOn": ["^build"]
    }
  }
}
